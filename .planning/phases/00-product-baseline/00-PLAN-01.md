---
phase: 0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Build.props
  - Directory.Packages.props
  - Shareguard-wpf.slnx
  - ShareGuard.Domain/ShareGuard.Domain.csproj
  - ShareGuard.Application/ShareGuard.Application.csproj
  - ShareGuard.Infrastructure/ShareGuard.Infrastructure.csproj
autonomous: true
requirements: []
---

<objective>
Establish the multi-project solution skeleton with centralized build configuration. Create the three class library projects (Domain, Application, Infrastructure) with correct dependency references, and set up Directory.Build.props and Directory.Packages.props for centralized settings and package management.
</objective>

<tasks>

<task id="01-01" type="execute">
<title>Create Directory.Build.props at solution root</title>
<read_first>
- Shareguard-wpf/Shareguard-wpf.csproj (current project settings to align with)
- docs/architecture.md (architectural constraints and tech stack)
</read_first>
<files>Directory.Build.props</files>
<action>
Create `Directory.Build.props` at the solution root (`c:\Users\ADMIN\source\repos\Shareguard-wpf\Directory.Build.props`) with the following content:

```xml
<Project>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>preview</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>
</Project>
```

Notes:
- Use `LangVersion=preview` since .NET 10 is preview and C# 14 may not be a recognized version string yet.
- `TargetFramework=net10.0` is the default; WPF projects override to `net10.0-windows`.
- `ManagePackageVersionsCentrally=true` enables Central Package Management.
</action>
<acceptance_criteria>
- File `Directory.Build.props` exists at solution root
- File contains `<TargetFramework>net10.0</TargetFramework>`
- File contains `<LangVersion>preview</LangVersion>`
- File contains `<Nullable>enable</Nullable>`
- File contains `<ImplicitUsings>enable</ImplicitUsings>`
- File contains `<ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>`
</acceptance_criteria>
</task>

<task id="01-02" type="execute">
<title>Create Directory.Packages.props for Central Package Management</title>
<read_first>
- Directory.Build.props (just created, confirms CPM is enabled)
- docs/architecture.md (tech stack section for package versions)
</read_first>
<files>Directory.Packages.props</files>
<action>
Create `Directory.Packages.props` at the solution root with all package versions needed for Phase 0:

```xml
<Project>
  <ItemGroup>
    <!-- MVVM -->
    <PackageVersion Include="CommunityToolkit.Mvvm" Version="8.4.0" />
    <!-- Hosting & DI -->
    <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.0-preview.5.25277.114" />
    <!-- Logging -->
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.0-preview.5.25277.114" />
    <!-- Testing -->
    <PackageVersion Include="xunit.v3" Version="1.1.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.13.0" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.0.2" />
    <PackageVersion Include="coverlet.collector" Version="6.0.4" />
  </ItemGroup>
</Project>
```

Note: If preview versions fail to restore, fall back to the latest stable versions available. The exact version numbers should be validated with `dotnet restore` after creation.
</action>
<acceptance_criteria>
- File `Directory.Packages.props` exists at solution root
- File contains `PackageVersion` entries for: `CommunityToolkit.Mvvm`, `Microsoft.Extensions.Hosting`, `xunit.v3`, `Microsoft.NET.Test.Sdk`
- No `Version` attributes appear in individual `.csproj` `PackageReference` entries (CPM enforced)
</acceptance_criteria>
</task>

<task id="01-03" type="execute">
<title>Create ShareGuard.Domain class library project</title>
<read_first>
- docs/superpowers/specs/2026-05-20-phase0-product-baseline-design.md (project structure definition)
- docs/architecture.md (Section 2.1 Core module layout)
- Directory.Build.props (inherited settings)
</read_first>
<files>ShareGuard.Domain/ShareGuard.Domain.csproj</files>
<action>
Create the `ShareGuard.Domain` project directory and csproj at the solution root level.

Run: `dotnet new classlib -n ShareGuard.Domain -o ShareGuard.Domain --no-restore`

Then replace the generated `.csproj` content with:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <!-- TargetFramework, LangVersion, Nullable, ImplicitUsings inherited from Directory.Build.props -->
</Project>
```

Delete the auto-generated `Class1.cs` file.

Create a placeholder marker file `ShareGuard.Domain/_readme.md`:
```
# ShareGuard.Domain

Platform-agnostic domain layer. Contains:
- Core entities (ShareItem, StripResult, Finding)
- Domain interfaces (IStripper, etc.)
- Domain exceptions

MUST NOT reference WPF, Win32, or any UI components.
```

This project has ZERO dependencies on other projects.
</action>
<acceptance_criteria>
- Directory `ShareGuard.Domain/` exists at solution root
- File `ShareGuard.Domain/ShareGuard.Domain.csproj` exists and is a valid SDK-style project
- `ShareGuard.Domain.csproj` does NOT contain `<TargetFramework>` (inherited from Directory.Build.props)
- No `Class1.cs` file exists in the project
- No `<ProjectReference>` entries exist in the csproj (zero dependencies)
</acceptance_criteria>
</task>

<task id="01-04" type="execute">
<title>Create ShareGuard.Application class library project</title>
<read_first>
- docs/superpowers/specs/2026-05-20-phase0-product-baseline-design.md (project structure — Application depends on Domain)
- ShareGuard.Domain/ShareGuard.Domain.csproj (the project it references)
- Directory.Build.props (inherited settings)
</read_first>
<files>ShareGuard.Application/ShareGuard.Application.csproj</files>
<action>
Create the `ShareGuard.Application` project:

Run: `dotnet new classlib -n ShareGuard.Application -o ShareGuard.Application --no-restore`

Replace the generated `.csproj` with:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\ShareGuard.Domain\ShareGuard.Domain.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>
</Project>
```

Delete the auto-generated `Class1.cs`.

Create the DI extension method file `ShareGuard.Application/DependencyInjection.cs`:

```csharp
using Microsoft.Extensions.DependencyInjection;

namespace ShareGuard.Application;

public static class DependencyInjection
{
    public static IServiceCollection AddApplicationServices(this IServiceCollection services)
    {
        // Application layer service registrations will be added here
        return services;
    }
}
```

Dependencies: Domain only. `Microsoft.Extensions.Logging.Abstractions` for `ILogger<T>` interface (no version — CPM managed).
</action>
<acceptance_criteria>
- Directory `ShareGuard.Application/` exists
- `ShareGuard.Application.csproj` contains `ProjectReference` to `ShareGuard.Domain.csproj`
- `ShareGuard.Application.csproj` contains `PackageReference` for `Microsoft.Extensions.Logging.Abstractions` WITHOUT a `Version` attribute
- No `Class1.cs` exists
- File `ShareGuard.Application/DependencyInjection.cs` exists and contains `public static IServiceCollection AddApplicationServices`
</acceptance_criteria>
</task>

<task id="01-05" type="execute">
<title>Create ShareGuard.Infrastructure class library project</title>
<read_first>
- docs/superpowers/specs/2026-05-20-phase0-product-baseline-design.md (Infrastructure depends on Domain + Application)
- ShareGuard.Domain/ShareGuard.Domain.csproj
- ShareGuard.Application/ShareGuard.Application.csproj
</read_first>
<files>ShareGuard.Infrastructure/ShareGuard.Infrastructure.csproj</files>
<action>
Create the `ShareGuard.Infrastructure` project:

Run: `dotnet new classlib -n ShareGuard.Infrastructure -o ShareGuard.Infrastructure --no-restore`

Replace the generated `.csproj` with:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\ShareGuard.Domain\ShareGuard.Domain.csproj" />
    <ProjectReference Include="..\ShareGuard.Application\ShareGuard.Application.csproj" />
  </ItemGroup>
</Project>
```

Delete the auto-generated `Class1.cs`.

Create the DI extension method file `ShareGuard.Infrastructure/DependencyInjection.cs`:

```csharp
using Microsoft.Extensions.DependencyInjection;

namespace ShareGuard.Infrastructure;

public static class DependencyInjection
{
    public static IServiceCollection AddInfrastructureServices(this IServiceCollection services)
    {
        // Infrastructure layer service registrations will be added here
        return services;
    }
}
```

Dependencies: Domain and Application.
</action>
<acceptance_criteria>
- Directory `ShareGuard.Infrastructure/` exists
- `ShareGuard.Infrastructure.csproj` contains `ProjectReference` to both `ShareGuard.Domain.csproj` AND `ShareGuard.Application.csproj`
- No `Class1.cs` exists
- File `ShareGuard.Infrastructure/DependencyInjection.cs` exists and contains `public static IServiceCollection AddInfrastructureServices`
</acceptance_criteria>
</task>

<task id="01-06" type="execute">
<title>Update Shareguard-wpf.csproj for Clean Architecture references</title>
<read_first>
- Shareguard-wpf/Shareguard-wpf.csproj (current state — bare WPF project)
- docs/superpowers/specs/2026-05-20-phase0-product-baseline-design.md (WPF depends on Application + Infrastructure)
- Directory.Build.props (inherited settings)
</read_first>
<files>Shareguard-wpf/Shareguard-wpf.csproj</files>
<action>
Replace the contents of `Shareguard-wpf/Shareguard-wpf.csproj` with:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <RootNamespace>ShareGuard.App</RootNamespace>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ShareGuard.Application\ShareGuard.Application.csproj" />
    <ProjectReference Include="..\ShareGuard.Infrastructure\ShareGuard.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="Microsoft.Extensions.Hosting" />
  </ItemGroup>

</Project>
```

Key changes:
- Override `TargetFramework` to `net10.0-windows` (WPF requires Windows TFM)
- Change `RootNamespace` from `Shareguard_wpf` to `ShareGuard.App`
- Add `ProjectReference` to Application and Infrastructure layers
- Add `PackageReference` for `CommunityToolkit.Mvvm` and `Microsoft.Extensions.Hosting` (no versions — CPM)
- Remove redundant `Nullable` and `ImplicitUsings` (inherited from Directory.Build.props)
</action>
<acceptance_criteria>
- `Shareguard-wpf.csproj` contains `<TargetFramework>net10.0-windows</TargetFramework>`
- `Shareguard-wpf.csproj` contains `<RootNamespace>ShareGuard.App</RootNamespace>`
- `Shareguard-wpf.csproj` contains `<UseWPF>true</UseWPF>`
- `Shareguard-wpf.csproj` contains `ProjectReference` to `ShareGuard.Application.csproj` and `ShareGuard.Infrastructure.csproj`
- `Shareguard-wpf.csproj` contains `PackageReference` for `CommunityToolkit.Mvvm` and `Microsoft.Extensions.Hosting` WITHOUT `Version` attributes
- `Shareguard-wpf.csproj` does NOT contain `ProjectReference` to `ShareGuard.Domain.csproj` (accessed transitively)
</acceptance_criteria>
</task>

<task id="01-07" type="execute">
<title>Update solution file to include all projects</title>
<read_first>
- Shareguard-wpf.slnx (current state — only has the WPF project)
</read_first>
<files>Shareguard-wpf.slnx</files>
<action>
Replace the contents of `Shareguard-wpf.slnx` with:

```xml
<Solution>
  <Project Path="ShareGuard.Domain/ShareGuard.Domain.csproj" />
  <Project Path="ShareGuard.Application/ShareGuard.Application.csproj" />
  <Project Path="ShareGuard.Infrastructure/ShareGuard.Infrastructure.csproj" />
  <Project Path="Shareguard-wpf/Shareguard-wpf.csproj" />
</Solution>
```

Test projects will be added in Wave 3.
</action>
<acceptance_criteria>
- `Shareguard-wpf.slnx` contains `Project` entries for all 4 projects: `ShareGuard.Domain`, `ShareGuard.Application`, `ShareGuard.Infrastructure`, `Shareguard-wpf`
- `dotnet restore Shareguard-wpf.slnx` completes without errors
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `dotnet restore Shareguard-wpf.slnx` — all packages resolve
2. Run `dotnet build Shareguard-wpf.slnx` — solution compiles with zero errors
3. Verify no circular references exist: Domain has 0 project refs, Application has 1 (Domain), Infrastructure has 2 (Domain, Application), WPF has 2 (Application, Infrastructure)
</verification>

<success_criteria>
- [ ] Directory.Build.props centralizes common settings for all projects
- [ ] Directory.Packages.props manages all NuGet versions centrally
- [ ] Four projects created with correct dependency direction
- [ ] Solution file references all projects
- [ ] `dotnet build` succeeds with zero errors and zero warnings
</success_criteria>

<must_haves>
- Clean Architecture project separation (Domain → Application → Infrastructure → App)
- No circular dependencies
- Central build configuration via Directory.Build.props
- Central package versioning via Directory.Packages.props
</must_haves>
